# Web-to-iOS Backend Handoffs

**Owner:** Web/shared-backend coding session  
**Consumer:** Native iOS coding session

The web session records only implemented and tested backend milestones here. A route's presence in OpenAPI alone does not mean it is ready for the native client.

## Current milestones

### Foundation contract — specified, not implemented

- Contract: `contracts/v2/openapi.json` version `0.1.0`
- Database: V2 staging migrations applied and catalog security audited
- API implementation: pending
- Native consumption status: use mocks only; do not assume live endpoints

## Handoff entry template

### Milestone name — `READY` or `BLOCKED`

- Date:
- Contract version:
- Staging base URL:
- Implemented operations:
- Authentication requirements:
- Feature flags or seed-data requirements:
- Automated verification:
- Known limitations:
- Minimum supported client version:
